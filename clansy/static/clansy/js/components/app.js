define([
    'react',
    ], function (React) {

    var App = React.createClass({
        render: function() {
            return (
                <main>
                    <h1>Reactin</h1>
                </main>
            );
        }
    });

    return App;
});
